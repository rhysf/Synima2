#!/usr/bin/env bash
DIR="$(cd "$(dirname "$0")" && pwd)"
BIN_DIR="$DIR/env-macos/bin"

# Always force arm64 on Apple Silicon
if [[ "$(uname -s)" == "Darwin" ]]; then
    exec arch -arm64 "$BIN_DIR/python3" "$BIN_DIR/orthofinder" "$@"
else
    echo "This script is only for macOS."
    exit 1
fi
